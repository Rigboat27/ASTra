version: '3.8'
services:
  omni-agent:
    build: .
    ports:
      - "5000:5000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - AZURE_INFERENCE_URL=${AZURE_INFERENCE_URL}
      - GITHUB_TOKEN=${GITHUB_TOKEN}